# 不要看这个md文件                                     你是资深Agent开发工程师，熟悉LangGraph、LangChain等相关技术栈，按最新版LangChain（v1.0+）写法，产出规范、可运行、注释清晰的代码，不冗余、不遗漏核心功能。请按以下需求，开发企业级AI智能客服机器人，核心数据用占位符（标注清晰），规避已知问题，确保Demo级可直接运行。这是一个专门针对机械革命笔记本电脑的智能客服机器人

### 要求如下
#### 1. 界面：Streamlit做一个简单的聊天界面，支持用户输入
### 2. 技术栈：
   - 大模型：支持本地ollama模型的qwen3.5 4b 和硅基流动的qwen3.5 4b(OPENAI_API_KEY=sk-mkvwihbbwuvirgxkdfmonkonurkpucfvdwvbhcjqlswwpvbr
     OPENAI_BASE_URL=https://api.siliconflow.cn/v1)   把这个两个模型放置在一个文件里，只使用本地模型，我想改时我会手动改代码的
   - 框架：LangChain + LangGraph
   - 向量库：Chroma
   - 嵌入：本地ollama模型的bge-m3

### 3.可以用langsmith来进行监控和分析，（api_key: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）
### 4. 功能：
   - RAG 知识库问答
   - 工具调用：用户问题解答、查询订单、查询物流、转接人工
   - 多轮对话记忆，使用redis实现记忆存储
   - 严格不编造答案
   - 只回答客服问题
   上下文管理：状态管理
   控制流设计：流程编排
   错误恢复：异常处理
   反馈回路：监控告警
### 5.项目结构

```
kefu_agent/
├── app.py                      # Streamlit Web 入口
├── api.py                      # FastAPI 接口
├── src/
│   ├── config/
│   │   ├── settings.py          # 配置加载 (dataclass)
│   │   └── logger.py            # 日志配置
│   ├── models/
│   │   └── types.py             # 数据类型定义
│   └── services/
│       ├── agent.py             # Agent 核心
│       ├── llm.py               # LLM 管理
│       ├── tools.py             # 工具定义
│       ├── memory.py            # 对话记忆
│       ├── rag.py               # RAG 知识库
│       ├── database.py          # JSON 数据层
│       └── intent.py            # 意图识别
├── data/
│   └── mock_data.json           # 模拟数据
├── tests/
│   └── test_services.py         # 单元测试
├── .env                         # 环境变量
├── .env.example                 # 环境变量模板
├── .gitignore                   # Git 忽略配置
├── requirements.txt             # 依赖
└── README.md
```
### 6. Agent 流程图


#### 整体分层架构（Layer Architecture）

┌─────────────────────────────────────────────────────────────┐
│                   接入层 (Access Layer)                      │
│  ┌───────────┐  ┌───────────┐  ┌─────────────────────────┐  │
│  │ 输入校验  │  │ 敏感词风控 │  │ 限流 & 恶意请求拦截     │  │
│  └───────────┘  └───────────┘  └─────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                   对话状态层 (Dialog State Layer)            │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐ │
│  │ 历史加载  │  │ 槽位管理  │  │ 实体追踪  │  │ 会话状态  │ │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘ │
├─────────────────────────────────────────────────────────────┤
│                   理解层 (Understanding Layer)              │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐ │
│  │ 意图识别  │  │ 槽位抽取  │  │ 实体链接  │  │ 澄清判断  │ │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘ │
├─────────────────────────────────────────────────────────────┤
│                   路由决策层 (Routing Layer)                 │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐ │
│ │ 闲聊回复 │ │ RAG 检索 │ │ 工具调用 │ │ 转人工 │ │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘ │
├─────────────────────────────────────────────────────────────┤
│                      执行层 (Execution Layer)                │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  │
│  │       RAG 执行链        │  │       工具执行链        │  │
│  │ 检索 → 重排 → 校验      │  │ 参数校验 → 调用 → 熔断  │  │
│  └─────────────────────────┘  └─────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                   融合生成层 (Generation Layer)             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │              多源信息合并 + 自然语言生成                ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│                      质检层 (Quality Layer)                 │
│  ┌───────────┐  ┌───────────┐  ┌─────────────────────────┐ │
│  │ 事实校验  │  │ 敏感信息擦 │  │       回复兜底           │ │
│  └───────────┘  └───────────┘  └─────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│                   埋点与持久化层 (Infra Layer)              │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐ │
│  │ 对话日志  │  │ 指标埋点  │  │ 状态存储  │  │ 异常监控  │ │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘ │
└─────────────────────────────────────────────────────────────┘

#### 状态机图（State Machine）—— 对话生命周期
plaintext
┌─────────────┐
│  空闲 (Idle) ◄───────────────────────────┐
└──────┬──────┘                            │
       │ 用户输入                          │
       ▼                                   │
┌─────────────────────┐                     │
│  理解与路由 (NLU+Routing) │                     │
└───────┬─────────────┘                     │
        │                                   │
┌───────▼───────┐  ┌──────────┐  ┌──────────▼─────────┐
│ 等待槽位填充  │◄─┤ 执行工具  │◄─┤ 混合执行(RAG+Tool) │
│ (WaitingSlot) │  │ (Tooling)│  │                    │
└───────┬───────┘  └──────────┘  └──────────┬─────────┘
        │                                   │
        └───────────┐                       │
                    │                       │
┌───────────────────▼────────────────────┐  │
│         生成回复 & 质检 (Generate)     │  │
└───────────┬───────────────────────────┘  │
            │                              │
            ▼                              │
┌─────────────────────────┐                │
│  转人工挂起 (Transfer)   │────────────────┘
└─────────────────────────┘
#### 核心模块依赖图（Component Diagram）

┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   agent.py      │     │   intent.py     │     │    rag.py       │
│ 主调度/Graph    │◄───►│ 意图/槽位/实体  │◄───►│ 向量库/检索     │
└────────┬────────┘     └─────────────────┘     └────────┬────────┘
         │                                              │
         │                                              │
         │     ┌─────────────────┐     ┌────────────────┘
         └────►│   memory.py     │◄────┘
               │ 对话状态/持久化 │
               └─────────────────┘
                        │
                        ▼
               ┌─────────────────┐
               │  Redis / 磁盘   │
               │  持久化存储     │
               └─────────────────┘
#### llm调用图（LLM Call Diagram）

用户输入
    ↓
记忆加载（历史、槽位、实体、状态）
    ↓
【1. 决策LLM（第1次，轻量）】
意图 / need_rag / need_tool / 槽位 / tool_call
（不生成人话）
    ↓
【Graph 执行层（全程 0 LLM）】
    ├─ need_rag → 同步检索原始文档
    └─ ReAct 循环：agent → tools → agent（直到无tool_call）
       仅执行、仅搬运、更新state
    ↓
汇总全部原料：历史+用户问题+RAG原文+工具原始结果
    ↓
【2. 生成LLM（第2次，最终话术）】
整合所有信息，输出自然回答
（不做决策、不抽槽）
    ↓
更新持久化记忆 → 返回用户
 '''




。